{
  "name": "@trynova-ai/auto-ids-js",
  "version": "1.0.1",
  "description": "Automatically inject ids into elements at runtime",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/trynova-ai/auto-ids-js.git"
  },
  "keywords": ["auto-ids-js"],
  "author": {
    "name": "Zach Smith",
    "email": "zach@trynova.ai"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
